<!DOCTYPE html>
<html lang="en">
  <head>
    <title>aldwin7894</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <meta http-equiv="content-type" content="text/html" />
    <meta charset="UTF-8" />

    <%= render "layouts/favicon" %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= vite_client_tag %>
    <%= vite_javascript_tag "admin", 'data-turbo-track': "reload" %>
  </head>

  <body>
    <div x-data="{ sidebarOpen: false }" class="font-roboto flex h-screen bg-gray-200">
      <%= render "/admin/shared/sidebar" %>

      <div class="flex flex-col flex-1 overflow-hidden">
        <%= render "/admin/shared/topbar" %>

        <main class="bg-gray-200n flex-1 overflow-x-hidden overflow-y-auto">
          <div class="container px-6 py-8 mx-auto">
            <%= yield %>
          </div>
        </main>
      </div>
    </div>

    <%= render partial: "partials/toasts" %>
    <%= yield :page_script %>
  </body>
</html>

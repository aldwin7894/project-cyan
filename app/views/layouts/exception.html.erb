<!DOCTYPE html>
<html lang="en">
  <!-- Head -->
  <head>
    <!-- Info -->
    <title>
      <%= "#{Rails.application.class.module_parent_name.downcase} | Error - #{@status} (#{@response})" %>
    </title>

    <!-- Styling -->
    <%= vite_client_tag %>
    <%= vite_stylesheet_tag 'stylesheets/exception.css.scss', media: 'all', 'data-turbo-track': 'reload' %>

    <%= favicon_link_tag vite_asset_path("images/favicon.png"), rel: "shortcut icon", type: "image/png" %>
    <%= favicon_link_tag vite_asset_path("images/apple-touch-icon.png"), rel: "apple-touch-icon", sizes: "152x152", type: "image/png" %>
    <%= favicon_link_tag vite_asset_path("images/apple-touch-icon-precomposed.png"), rel: "apple-touch-icon-precomposed", sizes: "152x152", type: "image/png" %>

    <!-- Auth -->
    <%= csrf_meta_tags %>
  </head>

  <!-- Body -->
  <body>
    <%= yield %>
    <%= render "layouts/cf" %>
  </body>
</html>

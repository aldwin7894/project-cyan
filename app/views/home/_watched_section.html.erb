<section class="flex-1 mb-20">
  <div class="container mx-auto">
    <turbo-frame id="watched_anime" src="<%= anilist_user_activities_path %>" loading="lazy">
      <%= render "watched_placeholder" %>
    </turbo-frame>
    <turbo-frame id="watched_movie" src="<%= anilist_user_activities_path %>" x-data="" loading="lazy">
      <%= render "watched_placeholder" %>
    </turbo-frame>
  </div>
</section>
